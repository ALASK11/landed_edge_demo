# TODO

- Revisit the redirect logic for unauthenticated users in `middleware.ts` to implement a more robust and secure authentication mechanism (e.g., using server-side session management instead of a client-side cookie).
